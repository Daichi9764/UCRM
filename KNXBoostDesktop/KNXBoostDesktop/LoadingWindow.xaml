<Window x:Class="KNXBoostDesktop.LoadingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:KNXBoostDesktop"
        Title="Chargement en cours..." Height="275" Width="500" WindowStartupLocation="CenterOwner"
        WindowStyle="None" ResizeMode="NoResize">
    <Window.Resources>
        <local:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <Style TargetType="ProgressBar" x:Key="NormalProgressBar">
            <Setter Property="Foreground" Value="#369226"/>
        </Style>
        <Style TargetType="ProgressBar" x:Key="GreenProgressBar">
            <Setter Property="Foreground" Value="#369226"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Border BorderBrush="#369226" BorderThickness="1.5">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="400">
                <TextBlock x:Name="taskNameText" Text="Initialisation..." FontSize="16" Margin="0,0,0,20" HorizontalAlignment="Center" />
                <ProgressBar x:Name="progressBar" Height="15" Margin="0,0,0,10" IsIndeterminate="True"/>
                <ListBox x:Name="activityLog" Height="110" Margin="0,10,0,20">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding Text}" Margin="5" VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="2" Text="✔" Margin="5,0,10,0" Visibility="{Binding IsCompleted, Converter={StaticResource BooleanToVisibilityConverter}}" Foreground="Green" VerticalAlignment="Center"/>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </StackPanel>
        </Border>
    </Grid>
</Window>
